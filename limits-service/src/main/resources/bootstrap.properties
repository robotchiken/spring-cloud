#El nombre de la aplicacion esta relacionado con el nombre del archivo de propiedades que se configuro en el servidor
spring.application.name=limits-service 
spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=dev